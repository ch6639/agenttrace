# CI：lint + 单元测试 + 覆盖率（NFR-5：CI 全绿方可合并）
# 覆盖率 70% 门槛已启用（TC-22；核心模块 model/context/storage 于 Sprint 1 落地）
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: lint+test (${{ matrix.os }} / py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: 安装依赖
        run: python -m pip install -e ".[dev]"

      - name: Lint（ruff）
        run: ruff check .

      - name: 测试 + 覆盖率（门槛 70%，NFR-5/TC-22）
        run: pytest --cov=agenttrace --cov-fail-under=70 --cov-report=term-missing --cov-report=xml

      - name: 上传覆盖率报告
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.os }}
          path: coverage.xml

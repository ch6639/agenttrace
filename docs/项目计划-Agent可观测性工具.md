# AgentTrace —— Agent 运行可观测性工具 · 项目计划

> 《软件工程》课程项目实践作业 · 单人 + AI 协同开发
> 截止：2026-12-31（当前 2026-09-08，剩余约 16 周）
> 占总评 35%

---

## 1. 项目定位

**一句话**：一个 Python SDK + Web 查看器。开发者加几行代码（装饰器），即可完整记录并回放自己 Agent 的每一步运行——LLM 调用、工具调用、决策步骤、Token 消耗、失败详情。

**解决什么问题**：Agent 系统是"技术黑箱"——课件明确指出 AI 生成代码带来"确认与验收难题"和"认知外包"风险；SWEBOK v4 新增"软件工程运营"知识领域。本工具让 Agent 的运行过程**可见、可查、可回放、可量化**。

---

## 2. 系统范围

### MVP 功能（验收标准逐条可测）

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| F1 | 非侵入式拦截 SDK | 装饰器/上下文管理器接入 ≤3 行代码，不改动 Agent 业务逻辑 |
| F2 | Trace 数据模型 | 一次 Agent 任务 = 一条 trace；LLM 调用/工具调用/步骤三类事件按树形时间线组织 |
| F3 | 持久化存储 | SQLite 落盘，进程重启后 trace 可查 |
| F4 | Web 查看器 | trace 列表页 + 时间线详情页（失败标红、耗时/Token 可见） |
| F5 | 统计总览 | 总 Token 成本、成功率、平均步数、失败类型分布 |
| F6 | 示例被测 Agent | 自带"课程学习助手"双工具 Agent（课程答疑 + 笔记整理，LLM 可用 mock 回放模式离线运行）作为演示载体 |

### 明确非目标（写进 SRS，防范围蔓延）

- 不做分布式/多进程追踪
- 不做 LangChain 等框架深度集成（列为展望）
- 不自研图表，前端用现成组件库
- 不做用户系统/权限

---

## 3. 系统架构

```
┌──────────────────┐
│  示例 Agent 应用   │  examples/demo_agent
│  （被观测对象）    │
└────────┬─────────┘
         │ ≤3 行接入（装饰器）
         ▼
┌──────────────────────────────┐
│  agenttrace SDK              │
│  ├─ interceptors  拦截 LLM/工具/步骤 │
│  ├─ model         Trace/Span 数据模型│
│  └─ storage       异步写入 SQLite    │
└──────────────┬───────────────┘
               ▼
          SQLite（trace.db）
               ▼
┌──────────────────────────────┐
│  server：FastAPI 查询服务      │  /traces  /traces/{id}  /stats
└──────────────┬───────────────┘
               ▼
┌──────────────────────────────┐
│  web：单页查看器（Vue 3）       │  列表 + 时间线回放 + 总览
└──────────────────────────────┘
```

**仓库结构**：
```
agenttrace/
├── agenttrace/        # SDK 包
├── server/            # FastAPI 服务
├── web/               # 前端查看器
├── examples/          # demo agent（演示用）
├── tests/             # pytest 单元+集成测试
├── docs/              # SRS、设计文档、测试报告（另存 Word 版）
├── models/            # PlantUML 源文件（用例图/组件图/类图/时序图）
└── .github/workflows/ # CI（GitHub Actions：lint + test + coverage）
```

---

## 4. 技术选型（附理由，写进设计文档）

| 项 | 选择 | 理由 |
|----|------|------|
| 语言 | Python 3.11+ | Agent 生态主流；课件/招聘要求均点名 Python |
| SDK 依赖 | 近零依赖（标准库优先） | 降低接入成本，本身就是卖点 |
| 存储 | SQLite | 单文件零部署，够用 |
| 服务 | FastAPI | 自动 OpenAPI 文档，省一页接口文档工作量 |
| 前端 | Vue 3 + Vite，图表用 ECharts | 单页应用，最快路径 |
| 测试 | pytest + coverage | TDD 支撑 |
| CI | GitHub Actions | 提交记录+CI 徽章=过程证据 |
| 建模 | PlantUML | 纯文本=天然"可编辑源文件"，满足作业第四条 |

---

## 5. 开发方法与 AI 协同策略

**方法**：单人 Scrum（1–2 周一个 Sprint）+ 核心模块 TDD。

**人机分工原则**（报告核心章节素材）：
- 人负责：数据模型设计、架构权衡决策、测试用例设计、AI 产出审查
- AI 负责：样板/适配代码、测试数据、前端页面骨架、文档初稿

**AI 协同证据留存**（每个 Sprint 更新）：
1. 关键 prompt 及其迭代版本（保留失败的第一版）
2. 构建的上下文说明（给了 AI 哪些文档/代码/约束）
3. AI 产出的人工修改 diff（前后对比截图）
4. 哪些产出被拒绝及原因

---

## 6. 里程碑（对齐 12-31 截止，含 2 周缓冲）

| 里程碑 | 时间 | 交付物 |
|--------|------|--------|
| M0 开题 | 第 1–2 周（~9/21） | 需获取记录（竞品分析 A.1）、SRS V0.5 内容完整稿、用例图（PlantUML）已完成；附录 B 追踪表已建立；教师确认暂缓（2026-09-10 决定，直接推进） |
| M1 设计 | 第 3 周（~9/28） | 架构/组件图、类图、数据模型、接口定义、技术选型报告 |
| M2 Sprint1 | 第 4–6 周（~10/19） | SDK 拦截 + 数据模型 + SQLite 存储 + demo agent（离线 mock 模式），单测覆盖；尾部垂直切片：造数 CLI + 只读 /api/traces + 列表页只读版（M1 决策 D-10，兼作 TC-19 数据源） |
| M3 Sprint2 | 第 7–9 周（~11/9） | FastAPI 服务 + Web 查看器（列表/时间线/总览），集成测试 |
| M4 测试强化 | 第 10–11 周（~11/23） | 异常/边界测试、SDK 开销性能评估（埋点前后的延迟对比数据）、缺陷记录与修复日志 |
| M5 交付 | 第 12–13 周（~12/7） | 测试报告、两个演示视频（运行使用 + 制品与开发过程介绍） |
| M6 收尾 | 第 14 周（~12/14） | 项目实践报告（Word 可编辑）、复盘章节（"如果重来一次怎么做得更好"） |
| 缓冲 | 12/14–12/31 | 应对意外 + 按提交要求整理目录（学号-姓名） |

> 注意：课后作业截止 11-13，与 M3/M4 重叠，提前排开精力。

---

## 7. 交付物清单（逐条映射作业要求）

- [ ] 代码仓库：源码 + 测试 + CI 配置 + README
- [ ] 需求规格说明书 SRS（Word，参照 GB/T 8567 或 IEEE 830）
- [ ] 设计文档：架构图/类图/时序图（Word 正文 + PlantUML 可编辑源文件）
- [ ] 测试报告：用例表、覆盖率、性能开销评估、缺陷日志
- [ ] 项目管理记录：GitHub Issues/Projects 导出、里程碑完成情况
- [ ] 问题解决日志：开发中遇到的 3–5 个典型问题及解决过程
- [ ] AI 协同记录：prompt 档案、diff 对比、人机分工说明
- [ ] 视频 1：运行与使用演示（时间线回放是核心画面）
- [ ] 视频 2：项目制品与开发过程介绍
- [ ] 项目实践报告（Word，含复盘与总结）
- [ ] 提交目录按"学号-姓名"命名

---

## 8. 风险与对策

| 风险 | 对策 |
|------|------|
| demo agent 依赖真实 LLM API（网络/费用/密钥） | SDK 内置 mock 回放模式，离线可演示；现场演示不赌网络 |
| 前端工作量失控 | 限定单页三视图；不追求美观，追求信息完整 |
| 范围蔓延 | 第 2 节"非目标"清单是硬约束，新想法一律进"展望"章节 |
| AI 生成代码质量隐患（课件：bug 多 41%） | 核心 trace/span/存储模块 TDD；CI 强制测试通过才合并 |

---

## 9. 当前状态与下一步（2026-09-17 更新）

已完成：SRS V0.5 内容完整稿（需求/用例/追踪表，2026-09-10）；**M1 概要设计定稿**——《M1设计文档-AgentTrace概要设计.md》（AT-DES-001）：总体架构与模块划分、SQLite 表结构 DDL、SDK/REST/CLI 接口定稿（已消掉 SRS"接口命名 M1 定稿"待办）、设计决策记录 D-01~D-10、Sprint 1 任务分解；模型图 6 张（新增组件图 + 摄入/流式/查询三张时序图，修订类图，沿用用例图/功能结构图）。设计输入《Langfuse 设计借鉴分析》（AT-DES-000）完成。四项关键设计决策已确认：显式拦截＋可选 openai 补丁（D-01）、瀑布图＋选中详情（D-03）、Element Plus（D-04）、垂直切片纳入 Sprint 1 尾部（D-10）。

SRS 遗留待办：作者署名/指导教师待回填（提交前统一处理）；NFR-1 阈值 M4 定稿；附录 A.2 dogfooding 记录自 Sprint 1 起持续填写；交付时转 Word 版（GB/T 8567 格式）。

暂缓项（按 2026-09-10 决定）：个人信息回填；教师选题确认。

下一步（M1 收尾 → Sprint 1 开工，约 2026-09-28 起）：
1. M1 文档渲染导出（PlantUML 图导出 PNG，供设计文档 Word 版使用）
2. 搭建 Git 仓库骨架（agenttrace/server/web/examples/tests 目录 + GitHub Actions CI）
3. Sprint 1 按 AT-DES-001 第 9 章 TDD 顺序推进（model → context → storage → pipeline → intercept → mock+demo → 垂直切片），开工即记录 dogfooding（SRS 附录 A.2）
